#!/usr/bin/env ruby
#^syntax detection

# ONLY USE THIS FOR MANAGING MODULES FROM THE FORGE. Do not use this for git 
# modules. That way, each module can be directly commited to the puppet repo.

forge "http://forge.puppetlabs.com"

mod 'puppetlabs/stdlib'
mod 'puppetlabs/ntp'
mod 'gentoo/portage'

